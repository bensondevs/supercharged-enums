# Common / Database

SQL database engine / driver names.

Namespace prefix: `BensonDevs\SuperchargedEnums\Common\Database\`

## Enums

### `DatabaseEngine`

`BensonDevs\SuperchargedEnums\Common\Database\DatabaseEngine` · backing: `string` · default: `Mysql` (`mysql`)

Common SQL database engines (driver names, not server versions). Backing values are lowercase English slugs.

| Case | Backing |
|------|--------|
| `Mysql` | `mysql` |
| `Mariadb` | `mariadb` |
| `Pgsql` | `pgsql` |
| `Sqlite` | `sqlite` |
| `Sqlserver` | `sqlserver` |

